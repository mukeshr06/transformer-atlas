export const lessons = [
{id:'attention',group:'01 — THE FOUNDATIONS',nav:'Why attention?',title:'Every word has a context.',description:'Start with a sentence. To understand “it”, a model needs to connect it to “animal”—even when other words lie between them.',pages:[35,36,37,38,39,40,41,42,69],formula:'A connection can span the whole sequence in one attention layer.',takeaway:'Self-attention lets each token collect information from other tokens. The strength of each connection depends on the current input.',steps:[['The sentence','Meaning depends on other words.','In “The animal didn’t cross the street because it was tired”, the meaning of “it” depends on a word earlier in the sentence. Select a token in the diagram to see its connections.'],['The bottleneck','A chain makes a long path.','A recurrent network carries a hidden state from one position to the next. Information about distant words must travel along that chain. The PDF’s falling memory percentages are a visual analogy, not a measured property of all recurrent networks.'],['The connections','Let positions communicate directly.','Self-attention builds a weighted connection between a query token and every allowed key token. Those connections are computed with matrices, so training can process many positions in parallel.'],['The update','Collect useful context.','The output for a token mixes information from other tokens. These illustrated links are explanatory; a real trained model learns its own patterns, and attention weights alone are not a complete explanation of its reasoning.']],deep:'<p>A token is a unit of text. A vector is an ordered list of numbers. A matrix is a rectangular collection of vectors. The next lessons build these ideas before using them to calculate attention.</p><p>Self-attention can connect distant positions in one layer, but it still has limits. A standard attention score matrix has n² entries per head. Doubling the sequence length quadruples that matrix. Parallel training does not make autoregressive generation parallel.</p>',note:'The source’s “no word forgotten” and memory bar are teaching metaphors, not guarantees.'},
{id:'tokens',group:'',nav:'Text → tokens',title:'Before words, there are tokens.',description:'A Transformer reads integer token IDs. A token might be a word, a piece of a word, punctuation, or a byte sequence.',pages:[1,43,44],formula:'text → tokenizer → [token ID₁, token ID₂, …, token IDₙ]',takeaway:'Tokenization is a fixed text-processing step. The model’s learned computation begins with the embedding lookup.',steps:[['Text','Begin with raw text.','The phrase “unbelievable attention” is just a sequence of characters. A tokenizer must turn it into units from its vocabulary.'],['Split','Words can become several pieces.','This illustration splits “unbelievable” into “un”, “believ”, and “able”. Real token boundaries depend on the chosen tokenizer; spaces and punctuation can be part of tokens.'],['IDs','Look up the vocabulary index.','Each piece maps to an integer ID. These illustrative IDs are labels, not numeric meanings: a larger ID does not mean a stronger or more important token.'],['Matrix','Make a sequence the model can read.','The sequence of IDs selects rows from an embedding table. This creates an n × d_model matrix: one vector for every token position.']],deep:'<p>Subword methods such as byte-pair encoding learn reusable text pieces from data. They balance vocabulary size against sequence length. This demo shows a conceptual split, not a specific production tokenizer.</p><p>Special tokens may mark the start or end of a sequence. Padded examples need a padding mask, so padding positions cannot contribute to attention. Token IDs and position IDs have different jobs.</p>',note:'The “The / cat / sat” screenshots use whole words and d_model = 4 for clarity.'},
{id:'embeddings',group:'',nav:'Embedding space',title:'Meaning becomes geometry.',description:'Every token selects a learned vector. Its coordinates provide a starting representation that later layers can change using context.',pages:[2,3,4,5,6,7,8,70],formula:'E ∈ ℝ^(vocabulary × d_model)     X[i] = E[token_idᵢ]',takeaway:'A token’s initial embedding is a learned lookup. Its contextual representation changes as it passes through the model.',steps:[['Lookup','One row for each token ID.','An embedding table holds a trainable vector for every vocabulary item. Repeated occurrences of a token begin with the same lookup vector before positional information is added.'],['Space','Coordinates encode learned features.','Vectors can be drawn as arrows in two or three dimensions. Real embeddings typically have many more coordinates, so the visible space is an illustration rather than a faithful projection of a trained model.'],['Relationships','Directions can express relationships.','The PDF illustrates vector offsets such as aunt minus uncle. Such analogies are approximate patterns found in some learned spaces, not universal laws. Embeddings can also reflect biases in training data.'],['Context','One token, different meanings.','“Mole” can describe an animal, an amount of substance, or a skin feature. The initial token embedding alone cannot choose the meaning. Attention and feed-forward layers use the surrounding tokens to update it.']],deep:'<p>The source’s 12,288-dimensional vectors refer to its GPT-3 example. The original Transformer base model used d_model = 512. Our live examples use four coordinates for readable arithmetic and three visible spatial axes.</p><p>Embedding entries start from an initialization and change during training. A coordinate does not normally have a single human-readable meaning. In the original Transformer, embeddings are multiplied by √d_model before adding positional encodings.</p>',note:'The 3D arrows are illustrative coordinates; they are not measured embeddings.'},
{id:'position',group:'',nav:'Position & order',title:'Give each token a place.',description:'Attention alone does not know which token came first. Positional information gives the model a way to represent order.',pages:[66,67],formula:'X₀ = √d_model · E[token_ids] + PE',takeaway:'Content and position enter the same vector. Position matters before the first attention layer.',steps:[['Order','The same tokens can say different things.','“The cat chased the dog” and “The dog chased the cat” contain the same kinds of tokens, but their order changes who did the chasing.'],['Waves','Build a different pattern at each position.','The original architecture uses sine and cosine waves with different frequencies. Read across a position to get a vector with d_model coordinates. Adjust the position slider to inspect that pattern.'],['Add','Add position to content.','The positional vector has exactly the same width as the token embedding. Add them element by element rather than appending extra coordinates.'],['Attend','Pass the sum into the first block.','Now queries and keys can depend on both token content and position. The model can learn patterns that respond to relative or absolute locations.']],deep:'<p>PE(pos, 2i) = sin(pos / 10000^(2i/d_model)); PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model)). The original model also tested learned positional embeddings. This lesson animates the sinusoidal version.</p><p>Without positions and without a causal mask, a self-attention layer is permutation equivariant: permuting the inputs permutes the outputs. Modern position techniques such as rotary embeddings work differently and should not be confused with additive sinusoids.</p>',note:'Added animation expands the positional-information sketches on PDF pages 66–67.'},
{id:'qkv',group:'02 — INSIDE ATTENTION',nav:'Queries, keys & values',title:'Three views of the same input.',description:'Learned projections turn each token vector into a query, a key, and a value. Each view plays a different role.',pages:[12,13,14,15,16,30,45,46,47,56],formula:'Q = XW_Q     K = XW_K     V = XW_V',takeaway:'Queries and keys decide the weights. Values carry the information that gets mixed.',steps:[['Input','Start with a matrix of token vectors.','Each row of X is the current representation of a token. In the toy example, “The”, “cat”, and “sat” produce a 3 × 4 matrix.'],['Query','Project what to look for.','Multiply X by W_Q to create queries. A query is a learned mathematical representation—not a literal question written in language.'],['Key','Project what can be matched.','Multiply X by W_K to create keys. Queries and keys must have the same width d_k so that their dot products are defined.'],['Value','Project what to pass along.','Multiply X by W_V to create values. W_Q, W_K, and W_V are learned parameters; Q, K, and V are temporary activations computed from the current input.']],deep:'<p>In this guide: X is n × d_model; W_Q and W_K are d_model × d_k; W_V is d_model × d_v. So Q and K are n × d_k, while V is n × d_v.</p><p>A matrix product is a collection of dot products: each output entry sums products of one input row and one weight column. The numerical lab uses d_model = 4 and d_k = d_v = 2. The source sometimes stores token vectors in columns, which transposes the multiplication order.</p>',note:'“Looking for” and “information offered” are analogies for learned projections.'},
{id:'scores',group:'',nav:'Dot-product scores',title:'Measure how well tokens match.',description:'Compare every query with every key. The resulting score matrix contains one row for every query position.',pages:[17,18,19,21,48,57],formula:'S = QKᵀ     S[i,j] = Σₖ Q[i,k] K[j,k]',takeaway:'Row i asks where token i should look. Column j corresponds to the token that may supply information.',steps:[['Query row','Choose one query.','Select “The”, “cat”, or “sat” below the scene. That token’s query vector will be compared with all three key vectors.'],['Key columns','Transpose the keys.','K has one key per row. Transposing it puts one key per column, making the product QKᵀ an n × n matrix.'],['Multiply & sum','Compute one score.','Multiply matching coordinates and add the products. The result can be positive or negative; raw attention scores are not probabilities.'],['All pairs','Repeat for every pair.','The full matrix calculates all query–key comparisons together. Our visual scan shows the arithmetic sequentially for teaching, although matrix operations can run in parallel.']],deep:'<p>For a single pair with q = [q₁,q₂] and k = [k₁,k₂], the score is q₁k₁ + q₂k₂. A dot product depends on both alignment and vector magnitude; it is not the same as cosine similarity unless vectors are normalized.</p><p>Self-attention uses n queries and n keys, producing n × n scores. Cross-attention with m decoder positions and n encoder positions produces m × n scores.</p>',note:'Rows are queries in the live lab; several earlier PDF diagrams use the transposed convention.'},
{id:'softmax',group:'',nav:'Scale & softmax',title:'Turn scores into a distribution.',description:'Scale dot products, apply any masks, then normalize each row. Every allowed key receives a nonnegative weight.',pages:[20,22,49,50,51,58],formula:'A = softmax(QKᵀ / √d_k + M)     Σⱼ A[i,j] = 1',takeaway:'Softmax compares scores within a query row. It produces relative weights, not independent yes/no decisions.',steps:[['Raw scores','Scores can get too large.','Dot products tend to grow in scale as the query and key dimension grows. Very large score differences can make softmax almost one-hot and gradients very small.'],['Scale','Divide by the square root of d_k.','The scaling factor keeps typical score magnitudes more manageable. Toggle scaling to compare the result. It changes the weights, not the shape of the matrix.'],['Normalize','Exponentiate and divide by the sum.','Softmax maps scores to positive weights. For numerical stability, subtract the row’s maximum before exponentiating; this leaves the final probabilities unchanged.'],['Distribution','Check that each row sums to one.','Inspect the percentages. Adjust temperature to see a sharper or smoother distribution. Temperature here is an extra teaching control, separate from the standard √d_k factor.']],deep:'<p>Stable softmax: aⱼ = exp(sⱼ − max(s)) / Σₖ exp(sₖ − max(s)). The mask is applied before this operation. A masked score of −∞ produces a zero weight.</p><p>The live lab computes these numbers from its fixed X, W_Q and W_K. It does not copy rounded percentages from unrelated PDF examples. Rounding may make displayed percentages sum to 99.9% or 100.1%; the underlying row sums to one.</p>',note:'PDF percentages and values belong to separate illustrative examples.'},
{id:'values',group:'',nav:'Mix the values',title:'The weights become an update.',description:'Multiply the attention weights by the value matrix. Each output token becomes a weighted mixture of value vectors.',pages:[26,27,28,29,31,32,52,53,59],formula:'Z = AV     Z[i] = Σⱼ A[i,j] V[j]',takeaway:'Attention mixes vectors, not words. The resulting numbers form a contextual update for each token.',steps:[['Weights','Use one row of attention weights.','Choose a query token. Its row tells us how much of each value vector to use. All three contributions are shown with their computed weights.'],['Values','Read the corresponding value vectors.','The values are produced by W_V. Their coordinates need not have the same dimension as queries and keys, though many implementations choose equal widths.'],['Weighted sum','Scale and add the contributions.','Multiply each value vector by its attention weight, then add the scaled vectors coordinate by coordinate. No word string is literally being averaged.'],['Output','Keep one output per query.','The attention output has n rows and d_v columns. Multi-head attention will concatenate these outputs and project them back to d_model.']],deep:'<p>If A is n × n and V is n × d_v, then AV is n × d_v. With nonnegative row-normalized weights, each single-head output lies in the convex hull of its value vectors before the output projection.</p><p>Some PDF diagrams fold an output projection into a large “value” map. Here W_V and W_O are separate: W_V produces values per head; W_O combines the concatenated heads. The residual connection is also separate from AV.</p>',note:'The source’s “weighted words” equations are conceptual shorthand for weighted value vectors.'},
{id:'context',group:'',nav:'Read an attention map',title:'See the pattern, one row at a time.',description:'A heatmap makes token-to-token weights visible. Read horizontally from a query to the keys that contribute to its output.',pages:[9,11,54,55,60],formula:'query position → one row     key position → one column',takeaway:'A bright cell means a larger mixing weight in that head and layer. It is not proof of linguistic understanding or causation.',steps:[['Axes','Locate the query and key axes.','The live map uses query rows and key columns. Select a query to highlight its row and inspect its weights.'],['Weights','Brightness represents relative weight.','Each cell shows how strongly that query draws from that key. The values are normalized independently for each query, so every row sums to one.'],['Context','Follow a token’s incoming information.','The updated token can carry information from faraway positions. It can receive a mix of multiple words and does not have to select exactly one.'],['Layers','Patterns can change with depth.','Every layer and every head can form a different attention matrix. A single heatmap is just one part of a much larger computation.']],deep:'<p>The six-token illustration computes row-normalized toy scores for “The cat sat on the mat”. It is not a heatmap captured from a trained language model. Click a cell to inspect its query, key and weight.</p><p>Attention scores describe one operation inside a model. Residual paths, value projections, later layers and output projections also determine the final prediction.</p>',note:'Illustrative attention patterns should not be interpreted as a model explanation.'},
{id:'heads',group:'',nav:'Multiple attention heads',title:'Look through several lenses.',description:'Run attention with several learned sets of projections. Concatenate the results, then mix them with an output projection.',pages:[33,34,61],formula:'MultiHead(X) = Concat(head₁, …, headₕ) W_O',takeaway:'Heads provide parallel representation subspaces. A head is not required to correspond to one human-readable meaning.',steps:[['Project','Create a different view for every head.','Each head has its own learned query, key and value projections. It receives the same input token representations but can compare different features.'],['Attend','Compute several attention maps.','Run scaled dot-product attention independently within every head. Switch the highlighted head below to inspect a different illustrative pattern.'],['Concatenate','Join the head outputs side by side.','Concatenation joins features, not sequence positions. The number of token rows stays the same while the feature width becomes h × d_v.'],['Project back','Mix heads back into the model width.','W_O maps the joined features back to d_model. That common width allows the result to be added to the residual stream.']],deep:'<p>Original base-model dimensions: 8 heads, d_model = 512, d_k = d_v = 64, so concatenation returns 512 features. Each head is narrower than the whole representation.</p><p>W_O has shape (h × d_v) × d_model. The source’s page 33 uses dimensions from a much larger GPT-3 example. Those numbers should not be mixed with the original base configuration.</p>',note:'The PDF’s “one head, one meaning” is a simplification; heads need not have fixed semantic roles.'},
{id:'mask',group:'03 — BUILD THE TRANSFORMER',nav:'Causal & padding masks',title:'The future stays hidden.',description:'A decoder predicts the next token using only the prefix. A causal mask prevents it from seeing later target positions.',pages:[23,24,25],formula:'M[i,j] = 0 if j ≤ i; otherwise −∞     A = softmax(S + M)',takeaway:'Mask scores before softmax. With row-query notation, the allowed region is the lower triangle, including the diagonal.',steps:[['Prefix','Shift the target sequence right.','For a target “the cat sat”, feed the decoder “<start> the cat” and ask it to predict “the cat sat”. The target for a position is not present as that position’s input.'],['Mask','Block future key positions.','The input token at position i can attend to positions up to and including i. All later key positions receive −∞ in the score matrix.'],['Normalize','Give blocked positions zero weight.','Softmax converts the masked scores into a distribution over allowed positions only. Toggle the mask to compare the two patterns.'],['Parallel training','Compute all training positions together.','With known shifted targets and a causal mask, training can evaluate many target positions at once without future leakage. At generation time, the next token is not known yet, so decoding remains sequential.']],deep:'<p>Causal masking and padding masking are different. Causal masks hide future positions. Padding masks hide artificial padding tokens used to batch unequal sequence lengths. They can be combined before softmax.</p><p>Encoder self-attention normally sees the full source sequence. Decoder self-attention is causal. Encoder–decoder cross-attention normally sees all non-padding source positions. The lab always preserves at least one allowed key per query.</p>',note:'Earlier source diagrams use query columns, so their masked triangle appears transposed.'},
{id:'norm',group:'',nav:'Residuals & normalization',title:'Keep the signal. Stabilize the scale.',description:'Residual connections add a sublayer’s update to its input. Layer normalization controls feature scale separately at each token.',pages:[62],formula:'y = LayerNorm(x + Dropout(Sublayer(x)))',takeaway:'A residual path preserves an additive route for information and gradients. LayerNorm works across features of one token, not across tokens.',steps:[['Input','Keep a direct path for x.','Send the current token representation both through the sublayer and along a skip path. The two paths must have the same width when they rejoin.'],['Update','Compute an attention or FFN update.','The sublayer produces new features. During training, dropout can randomly zero some update coordinates with scaling; dropout is disabled during ordinary inference.'],['Add','Add the update to the original.','Elementwise addition forms x + update. The residual stream can carry existing information through many layers while new blocks make adjustments.'],['Normalize','Normalize across this token’s features.','Compute the mean and variance over the feature coordinates, standardize with a small epsilon, and apply learned scale and shift. Other token positions are normalized independently.']],deep:'<p>LayerNorm(x) = γ ⊙ (x − μ) / √(σ² + ε) + β, with μ and σ² computed over the feature dimension. γ and β are learned. The lab shows the standardized values with γ = 1, β = 0.</p><p>The original 2017 architecture uses post-norm, shown here. Many later models use pre-norm: x + Sublayer(LayerNorm(x)). Neither convention should be silently substituted for the other in an architecture diagram.</p>',note:'Added diagram makes the small Add & Norm block in the PDF explicit.'},
{id:'ffn',group:'',nav:'Feed-forward networks',title:'Mix information. Then transform it.',description:'After attention mixes across positions, a feed-forward network transforms each token’s features independently.',pages:[64,68],formula:'FFN(x) = ReLU(xW₁ + b₁) W₂ + b₂',takeaway:'The same feed-forward weights are applied to every token in a layer. Different layers have their own weights.',steps:[['Per token','Take one contextual token vector.','Attention has already moved information between positions. The feed-forward sublayer now works separately on each token’s feature vector.'],['Expand','Project to a wider hidden layer.','The first affine transformation maps d_model features to d_ff features. The original base Transformer expands from 512 to 2048.'],['Nonlinearity','Apply a nonlinear activation.','The original model uses ReLU, which replaces negative values with zero. This nonlinearity makes the two projections more expressive than one linear map.'],['Contract','Project back to the residual width.','The second affine transformation maps d_ff back to d_model. Dropout, a residual addition and normalization wrap the sublayer in the original architecture.']],deep:'<p>The toy network shows 4 → 8 → 4 features. Each connection represents a learned weight, and every hidden unit also has a bias. The connection pulses and zeroed units illustrate the operations; this drawing is schematic.</p><p>The FFN does not exchange information between token positions. Its parameters are shared across positions within one layer. Modern models often change the activation or use gated feed-forward layers; this lesson retains the original ReLU structure.</p>',note:'Added expansion and activation animation complements the PDF’s block-level network.'},
{id:'architecture',group:'',nav:'Encoder & decoder',title:'Now assemble the whole machine.',description:'The encoder builds a source representation. The decoder combines a target prefix with that representation to predict the next output token.',pages:[63,65],formula:'source → encoder memory → decoder cross-attention → output distribution',takeaway:'Cross-attention is the bridge: queries come from the decoder; keys and values come from the encoder output.',steps:[['Inputs','Embed both sequences and add positions.','The encoder receives the source sequence. The decoder receives the shifted target during training or the generated prefix at inference time. Each stream gets its own positional information.'],['Encoder','Build contextual source representations.','An encoder block contains self-attention, Add & Norm, feed-forward, and Add & Norm. Stack N blocks; the original base model uses N = 6. Every source token can attend to all unmasked source positions.'],['Decoder','Use the prefix and the source.','A decoder block applies masked self-attention, Add & Norm, cross-attention, Add & Norm, feed-forward, and Add & Norm. The cross-attention keys and values come from the final encoder output.'],['Output','Project the final decoder state.','A linear layer maps each final decoder representation to vocabulary logits. Softmax gives a next-token distribution. Generation feeds the selected token back into the prefix.']],deep:'<table><tr><th>Attention kind</th><th>Queries</th><th>Keys and values</th><th>Visibility</th></tr><tr><td>Encoder self-attention</td><td>Encoder state</td><td>Same encoder state</td><td>All source tokens</td></tr><tr><td>Decoder self-attention</td><td>Decoder state</td><td>Same decoder state</td><td>Prefix only</td></tr><tr><td>Cross-attention</td><td>Decoder state</td><td>Final encoder output</td><td>All source tokens</td></tr></table><p>Residual Add & Norm wraps each sublayer. A decoder block therefore has three such wrappers; an encoder block has two. Repeated layers have the same structure but distinct learned parameters.</p>',note:'The large diagram follows the original post-norm encoder–decoder, not a decoder-only LLM.'},
{id:'generation',group:'04 — LEARNING & GENERATION',nav:'Predict the next token',title:'One distribution. One more token.',description:'The final hidden state becomes vocabulary logits. Choose a token, append it to the prefix, and repeat.',pages:[10],formula:'p(next token) = softmax(h_final W_vocab + b)',takeaway:'A next-token prediction is a probability distribution over the vocabulary. A complete sequence is generated by repeating that step.',steps:[['Hidden state','Read the last position.','During generation, use the final representation at the most recent input position. During training, a prediction can be computed at every non-padding position.'],['Logits','Project to the vocabulary.','The output matrix maps d_model features to one score for each vocabulary item. Scores are unnormalized logits and can have any real value.'],['Select','Turn scores into a token choice.','Softmax produces probabilities. Greedy decoding chooses the largest. Sampling draws from the distribution; temperature changes its sharpness. The live example uses fixed illustrative logits.'],['Append','Add the token and run again.','The new token becomes part of the prefix. Repeat until an end-of-sequence token or a length limit. A key–value cache can reuse earlier attention keys and values rather than recomputing them.']],deep:'<p>The output vocabulary matrix has shape d_model × vocabulary_size. Some models tie it to the transpose of the embedding matrix. This is separate from the per-head W_V value matrix.</p><p>A decoder-only model has no encoder–decoder cross-attention. It generates with causal self-attention and feed-forward blocks. The original encoder–decoder instead conditions on encoder memory throughout generation.</p>',note:'This browser demo animates a fixed sequence; it does not run a pretrained text generator.'},
{id:'training',group:'',nav:'How the model learns',title:'Prediction becomes a learning signal.',description:'Training compares predictions with known target tokens. Gradients tell the optimizer how to adjust the model’s parameters.',pages:[],formula:'L = − mean(log p(correct token))     θ ← optimizer(θ, ∇θL)',takeaway:'The architecture specifies the computation. Training learns the numbers in embeddings, projections, feed-forward networks and normalization parameters.',steps:[['Examples','Provide a correct continuation.','Training examples provide source and target text for translation, or text prefixes and next-token targets for language modeling. Shift targets so the model cannot simply read its answer.'],['Forward pass','Compute next-token probabilities.','Run the embedding, positions, attention and feed-forward computation. Compare each prediction with its known target using cross-entropy, excluding padding positions from the loss.'],['Backward pass','Differentiate the loss.','Backpropagation applies the chain rule through the computation. A gradient describes how a small parameter change would affect this batch’s loss. It is not a hand-written language rule.'],['Update','Adjust parameters and repeat.','An optimizer such as Adam uses gradients to update parameters. Many batches gradually improve the fit. Held-out validation checks whether improvements generalize beyond the training examples.']],deep:'<p>The animation is a schematic training loop, not a live training run. Lowering the probability assigned to the true target increases −log(p). For p = 0.8, the loss is about 0.223 nats; for p = 0.1, it is about 2.303.</p><p>The original training recipe used Adam, a warmup then decay learning-rate schedule, dropout and label smoothing. During ordinary inference, parameters stay fixed, gradients are not required, and dropout is disabled. Attention activations still change for each new input.</p>',note:'Added lesson: the PDF does not fully explain optimization and training.'},
{id:'walkthrough',group:'',nav:'The complete journey',title:'Follow a token, end to end.',description:'Put the pieces together: turn text into representations, exchange context, transform features, and predict what comes next.',pages:[],formula:'token IDs → embeddings + positions → N blocks → logits → probabilities',takeaway:'A Transformer is a stack of learned, differentiable vector operations. Attention handles communication; feed-forward layers transform each position.',steps:[['Represent','Turn text and position into vectors.','Tokenize the input, look up embeddings, and add positional information. The resulting sequence is a matrix with one row per token.'],['Communicate','Mix information through attention.','Project queries, keys and values. Calculate scaled scores, apply masks, normalize with softmax, and mix the values. Concatenate the heads and project back.'],['Transform','Add, normalize, and transform.','Add the update to the residual stream and normalize. Run the feed-forward network, then another residual addition and normalization. Repeat through the stack.'],['Predict & learn','Use the same core in different settings.','An encoder–decoder adds cross-attention to condition on a source. A decoder-only model predicts from a prefix; an encoder-only model uses bidirectional representations for suitable tasks. Training adjusts weights; generation repeatedly selects tokens.']],deep:'<table><tr><th>Model family</th><th>Core visibility</th><th>Typical role</th></tr><tr><td>Encoder-only</td><td>Bidirectional input context</td><td>Representations and classification</td></tr><tr><td>Decoder-only</td><td>Causal prefix</td><td>Autoregressive text generation</td></tr><tr><td>Encoder–decoder</td><td>Source context + causal target</td><td>Conditional sequence generation</td></tr></table><p>Final shape check: a block preserves n × d_model. Attention creates n × n score matrices per head. The output head expands the feature width to the vocabulary size. Autoregressive generation extends the sequence one token at a time.</p>',note:'Use the source collection to revisit any of the 70 images in its matching lesson.'}
];

export const sourceInfo = [
['Tokens are not always words','The source contrasts whole-word boxes with subword pieces. Follow the sentence, then the smaller token boundaries.','rows'],
['Tokens become vectors','Each token selects a numeric embedding. The final position will eventually produce a next-token prediction.','columns'],
['Words → vectors','Text is represented as vectors. The source moves from the word list to a geometric embedding space.','split'],
['Vector relationships: aunt and uncle','The arrows illustrate approximate relationships in an embedding space. They are examples rather than universal equalities.','vectors'],
['Vector relationships: daughter and son','Compare the offset between two word vectors with another offset. Learned relationships can reflect training-data biases.','vectors'],
['Context changes a representation','The token “King” is enriched with context. The arrows are a conceptual illustration of information added by later layers.','vectors'],
['Three meanings of “mole”','The same spelling can refer to an animal, a quantity, or a skin feature. Surrounding text distinguishes the meanings.','rows'],
['One initial embedding, multiple contexts','The occurrences start with a shared token embedding. Their later contextual vectors can become different.','rows'],
['A sequence enters attention','The source turns a token sequence into a stack of vectors and sends them through attention.','stack'],
['The final vector predicts a token','The highlighted final representation is projected to a vocabulary distribution. The bars represent possible continuations.','split'],
['Context flows into the final token','Curved connections show how earlier positions can contribute information to a later representation.','focus'],
['Contextualizing “fluffy blue creature”','The token vectors enter learned transformations. A contextual update can collect features from neighboring tokens.','rows'],
['A learned query projection','The matrix W_Q turns embedding features into a query vector. “Any adjectives?” is a teaching analogy.','rows'],
['From embedding space to query space','A learned linear projection maps a large embedding to a narrower query/key space.','split'],
['One token asks a query','Focus on the query derived from “creature”, then follow the matrix multiplication below it.','rows'],
['Every token gets a query','The same learned query matrix is applied to every token in the sequence.','rows'],
['Pairwise query–key scores','Every query is compared to every key. This source places queries in columns and keys in rows.','matrix'],
['Inspecting a score column','A highlighted query column compares one token with all candidate keys. The live lab uses the transposed convention.','matrix'],
['An attention score pattern','Matching queries and keys produce large or small scores. These raw numbers are not yet probabilities.','matrix'],
['Weights must sum to one','The bar chart introduces nonnegative weights with a sum of one. Softmax converts scores into this distribution.','split'],
['The scaled attention equation','This source uses a column-vector convention, displaying KᵀQ. The live lessons use row vectors and QKᵀ.','matrix'],
['Softmax produces an attention pattern','Normalization turns pairwise scores into relative mixing weights. The normalization axis follows the source’s column convention.','matrix'],
['Predict from a prefix','A language model predicts the next token using only tokens already available in the prefix.','rows'],
['A longer prefix changes the distribution','More context changes which next-token continuations receive probability.','rows'],
['Masking future tokens','Unavailable future connections are marked out. The triangle is transposed relative to a row-query diagram.','matrix'],
['Values carry the information','A learned value map turns token features into information that can be passed to other positions.','split'],
['Inside a value projection','The highlighted row and column demonstrate a dot product inside matrix multiplication.','split'],
['The value matrix','The same value projection is applied to each token. Values are distinct from the keys used to score matches.','split'],
['A weighted contextual update','Weights scale the value vectors, which are added into an update. The source illustrates a creature becoming blue and fluffy.','split'],
['Three sets of learned weights','Query, key and value matrices play different roles. Their activations depend on the current sequence.','rows'],
['A factorized linear map','A narrow intermediate representation can factor a much larger linear transformation.','focus'],
['Understanding the overall map','The source combines its projection steps into one overall transformation. The lessons keep W_V and W_O separate.','split'],
['A GPT-3 parameter example','These dimensions describe the source’s GPT-3 example, not every Transformer and not the original base model.','rows'],
['Many heads in parallel','Separate projection matrices create several attention patterns. Their output features are combined after attention.','stack'],
['Sequential processing','A recurrent model updates a hidden state one token at a time. The bar is a visual analogy for long-range information.','rows'],
['An ambiguous pronoun','To interpret “it”, the model must use an earlier word. The displayed memory percentage is illustrative.','rows'],
['The long-dependency problem','Distant information travels through many recurrent updates. The image dramatizes a possible difficulty, not a universal failure.','rows'],
['What if all positions could interact?','Self-attention allows direct token-to-token communication within the permitted context.','rows'],
['Tokens become a graph','The sentence positions are shown as nodes. Attention weights will specify the strength of their connections.','network'],
['Connecting “it” and “animal”','The highlighted link illustrates a context relationship. A trained model’s actual weights may differ.','network'],
['Introducing self-attention','The source introduces the 2017 architecture. Parallel communication helps with long dependencies but does not guarantee perfect memory.','rows'],
['From the idea to the math','The next step is to replace the verbal analogy with vector projections and matrix arithmetic.','rows'],
['A four-dimensional toy embedding','“The”, “cat” and “sat” each receive a four-coordinate vector. Real models usually use more coordinates.','columns'],
['The input matrix X','The three token vectors form a 3 × 4 matrix, with token positions in rows.','focus'],
['Branching into Q, K and V','The input is projected through three learned matrices, producing queries, keys and values.','branches'],
['Computing XW_Q','Each output query coordinate is a dot product of an input row and a weight column.','split'],
['All three projections','Q, K and V share the input but use separate learned weights. The shown arithmetic is a toy example.','columns'],
['Computing QKᵀ','Multiply query rows by key columns to obtain one score for every query–key pair.','split'],
['Why scale by √d_k?','Unscaled dot products can make softmax very sharp. Scaling helps keep the score distribution manageable.','split'],
['Softmax attention weights','A query’s scaled scores become nonnegative weights whose sum is one.','split'],
['A full weight matrix','Read each row as the distribution for one query. The three rows are normalized independently.','matrix'],
['Attention weights times values','Multiply a query’s attention row by the value matrix to obtain one output vector.','split'],
['Contextualized token outputs','These expressions are conceptual shorthand: attention mixes numeric value vectors, not literal word strings.','rows'],
['A six-token attention heatmap','Query rows compare against key columns. Color represents a weight, with each row normalized separately.','matrix'],
['Reading the heatmap diagonal','Diagonal entries show self-contributions; off-diagonal entries show information taken from other positions.','matrix'],
['Three roles from one matrix','Queries match keys; values carry content. The labels are intuitive descriptions of learned projections.','branches'],
['The score computation','QKᵀ compares every pair, and division by √d_k rescales the results.','split'],
['The softmax operation','Apply normalization row by row to turn scaled scores into an attention-weight matrix.','split'],
['The full attention pipeline','Follow projections, scores, scaling, softmax and the weighted-value product in order.','pipeline'],
['A pronoun in context','The sentence shows a useful semantic relationship. Attention can carry relevant context between positions.','focus'],
['“Bank” in two contexts','Financial and geographic uses illustrate contextual meaning. Individual heads need not specialize in one meaning each.','rows'],
['Residual addition and normalization','The small source diagram places operations in the architecture. The added lesson expands these into a readable animation.','focus'],
['The full Transformer architecture','The encoder processes a source; the decoder combines a prefix with encoder output. Cross-attention connects them.','architecture'],
['Expanding attention and feed-forward','Detailed views reveal the operations inside the high-level blocks. The network acts independently on each position.','architecture'],
['Blocks within the architecture','Follow the main model into its attention and feed-forward sublayers. Residual connections wrap the operations.','architecture'],
['Embeddings plus positional information','The source adds position to token representations before entering the model. The live lesson shows the wave construction.','pipeline'],
['Input representations enter the stack','Token and position information flow into repeated model blocks, which preserve the representation width.','pipeline'],
['Attention and feed-forward alternate','Attention mixes between positions. A shared feed-forward network transforms each position separately.','rows'],
['From recurrent models to Transformers','The comparison introduces a different architecture for sequence computation. Each family still has its own tradeoffs.','split'],
['Token positions and embedding coordinates','Positions label sequence order; embedding coordinates represent learned token features. The two axes have different meanings.','columns']
];

export function sourceLesson(page){return lessons.findIndex(l=>l.pages.includes(page));}
export const palette = {teal:'#90e8d2',blue:'#71b9eb',gold:'#e8ca7c',red:'#e18c83',purple:'#b6a0e8',text:'#e1e9eb',muted:'#748b9b'};
